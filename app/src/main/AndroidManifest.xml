<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<manifest package="javier.gradletransfuse" t:tag="+" xmlns:t="http://androidtransfuse.org" xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:label="@string/app_name" android:name=".GradleTransfuseApplication" android:theme="@style/AppTheme" t:tag="+,ab,i,l,n,t">
        <activity android:label="@string/app_name" android:name=".HelloWorldActivity" t:tag="+,l,n">
            <intent-filter t:tag="+">
                <action android:name="android.intent.action.MAIN" t:tag="+,n"/>
                <category android:name="android.intent.category.LAUNCHER" t:tag="+,n"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
